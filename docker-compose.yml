services:
  nba-predict:
    build: .
    image: cliprob/nba-predict:latest
    working_dir: /app
    command: make reproduce

  nba-predict-dev:
    build: .
    image: cliprob/nba-predict:latest
    working_dir: /app
    profiles:
      - dev
    volumes:
      - .:/app
    command: make reproduce
